{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Current File",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            },
            "envFile": "${workspaceFolder}/.env",
            "args": [
                // "-q",
                "--download",
                // "--no-categorize",
                // "--no-sync",
                // "--no-retrieve",
                // "--no-view",
                // "--no-archive",
                // "--direct_sync",
                // "-o",
                // "local=/Users/leo/Pictures/downloads拷貝3",
                // "rsync=-a -v"
                // "category=others"
                ]
        }
    ]
}
